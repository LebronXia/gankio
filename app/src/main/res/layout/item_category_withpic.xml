<?xml version="1.0" encoding="utf-8"?>
<android.support.v7.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
              xmlns:app="http://schemas.android.com/apk/res-auto"
              android:orientation="vertical"
              android:layout_width="match_parent"
              android:layout_height="wrap_content"
              app:contentPadding="0dp"
              app:cardCornerRadius="@dimen/card_corner_radius"
              app:cardElevation="@dimen/card_elevation"
    >

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="12dp"
        >

    <ImageView
        android:id="@+id/catedata_pic_iv"
        android:layout_width="match_parent"
        android:layout_height="120dp"
        android:scaleType="centerCrop"
        android:src="@mipmap/ic_zhanzheng"
        />

    <TextView
        android:id="@+id/catedata_via_tv"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/catedata_pic_iv"
        android:layout_marginTop="9dp"
        android:text="代码家"
        android:textSize="12dp"
        android:textColor="@color/item_date"
        />

        <TextView
            android:id="@+id/tv_data_date"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignBottom="@id/catedata_via_tv"
            android:layout_alignParentEnd="true"
            android:layout_alignParentRight="true"
            android:text="222"
            android:textColor="@color/item_date"
            android:textSize="@dimen/item_date_tv"/>
    </RelativeLayout>

</android.support.v7.widget.CardView>